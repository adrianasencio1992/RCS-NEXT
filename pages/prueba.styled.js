import styled from "styled-components";

const SCPrueba = styled.section`
 overflow-x: hidden;
 font-family: 'Arial';
 font-size:15px;
 
  }
body, html, #container {
  width: 100%;
  height: 100%;
  overflow:hidden;
}

div, img {
  position:absolute;
  user-select: none;
  overflow:hidden;  
}

`;

export default SCPrueba;
